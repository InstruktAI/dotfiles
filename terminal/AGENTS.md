# terminal/ — Agent Context

## Purpose

Cross-platform dark/light appearance management (`bin/appearance.py`) plus `tmux.conf`.

## Required reads

- `../README.md`, section "`terminal/` — Appearance Management": the platform seam,
  mode detection, commands, configuration, and the install / install-runtime split.

Thin wrapper only. All knowledge lives in `README.md`.
