# Dotfiles — Agent Context

## Purpose

Personal dotfiles that bootstrap a `zsh`/`tmux` terminal environment across macOS and
Linux, with synchronized dark/light theming across the terminal, tmux, and AI CLIs.

## Required reads

- `README.md` — the single source of truth: installation, repository layout, the
  appearance system, and how each part works.

Thin wrapper only. All knowledge lives in `README.md` — do not restate architecture,
commands, or configuration here.
