# zsh/ — Agent Context

## Purpose

Modular zsh configuration, loaded in numeric order, symlinked to `~/.config/zsh`.

## Required reads

- `../README.md`, section "`zsh/`": load order and each file's responsibility.

Thin wrapper only. All knowledge lives in `README.md`.
